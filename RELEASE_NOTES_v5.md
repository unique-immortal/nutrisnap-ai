# v5.0.0 — 本地优先架构 · 13 项修复 · 全新体验

> **线上地址**：https://nutrisnap-ai-940406235442.us-central1.run.app  
> **发布时间**：2026-05-20  
> **Commit**：`1bd0840`

---

## 🏗️ 架构升级：本地优先（Local-First）

数据不再全部上传服务器。仅同步每日汇总，单条食物/运动/聊天记录均保存在本地。

| 数据 | 存储 |
|------|------|
| 🍔 食物记录 | 本地 localStorage |
| 🏃 运动记录 | 本地 localStorage |
| 💬 聊天记录 | 本地 localStorage |
| 📊 每日汇总 | 服务器（跨设备同步） |
| 🧬 BMR 身体数据 | 服务器 |
| 🔐 账号密码 | 服务器 |

---

## 🔴 紧急 Bug 修复

- **卡路里仪表盘公式混淆** — 改为 `摄入 − 运动 = 净摄入`，`目标 → 剩余` 双行清晰布局。Header 火焰图标显示剩余额度。
- **周报平均值计算错误** — 改为仅计有记录天数，不再固定除以 7。显示「基于 X 天记录」。
- **本周运动总计始终为 0** — 修复数据未关联到报告页。
- **语音弹窗错别字** — 两处「吃了什么」。
- **AI 教练 Markdown 不渲染** — 回复中的 `**加粗**` `*列表*` 正常显示。

---

## 🟡 逻辑体验优化

- **AI 建议数据门槛** — 记录 < 3 天时显示「数据积累中」引导卡片，避免误判营养不良。
- **删除确认显示摘要** — 删除时展示「汉堡包 250kcal」等被删项详情。
- **底部 + 按钮改弹窗** — 点击弹出「记饮食 / 记运动」二选一菜单。
- **运动肌群选择逻辑确认** — 有氧运动隐藏肌群，仅力量训练显示。

---

## 🟢 UI/UX 打磨

- **时间戳友好化** — 今天/昨天/月日，去掉秒。
- **BMR 空状态优化** — 未设置时显示 `当前目标 2000 kcal（默认值）`。
- **深色模式去重** — 移除首页 header 图标，仅保留个人中心入口。
- **分量步进标注** — 按钮 hover 提示 ±25g。

---

## 🔧 技术变更

- **模型降级链**：gemini-3.5-flash → 2.5-flash → 2.5-lite → 2.0-flash → 3.1-lite
- **API 版本**：`/api/health` 返回 `"version": "v5-local-first"`, `"architecture": "local-meals + server-daily-summaries"`
