{
  "name": "nutrisnap-ai",
  "version": "1.0.0",
  "description": "NutriSnap AI Diet Tracker",
  "scripts": {
    "build": "mkdir -p www && copy templates\\index.html www\\index.html && xcopy static www\\static /E /I /Y"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
