# v5.6.56

- 语音记录改为优先使用多模态模型直接理解音频并输出饮食/运动结构化结果，不再把 `/api/voice-audio` 绑定到单独 STT 前置链路。
- 高级模式允许 `gpt-5.2` 接收音频输入；如果高级供应商不支持当前音频格式，会继续回落到 Google AI Studio 音频多模态链路。
- 前端语音上传失败后不再自动绕回旧的 `/api/speech-to-text` 链路，避免再次出现“语音转写服务繁忙”的误导提示。
- 语音分析元数据增加 `mode=direct_audio_model`，便于查看一次按钮点击实际触发了哪条模型链。
