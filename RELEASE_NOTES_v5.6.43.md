# Release v5.6.43

## Fixes

* 修复拍照识别在免费视觉模型额度耗尽后直接失败的问题，现已接入 Google 视觉兜底链路。
* 修复包装食品名称精修阶段未接入兜底的问题，降低“识别到食物但最终报错”的概率。
* 修复客户端语音入口优先走不稳定原生插件导致的卡住问题，默认改回更稳的录音上传转写链路。
* 优化语音转写顺序，免费模式优先使用 Gemini 转写，避免 OpenRouter 音频余额限制带来的失败和等待。
* 统一音频 MIME 处理，提升 `audio/webm;codecs=opus` 等手机端录音格式的兼容性。
* 当拍照模型链整体耗尽时，接口会返回可恢复的友好提示，不再直接抛 500。
