# v5.6.37

- 修复移动端语音入口优先拿不到原生 `SpeechRecognition` 插件的问题，改为通过 `registerPlugin('SpeechRecognition')` 获取并使用系统语音识别。
- 提升语音兜底成功率：本地只要识别出较短文本也可继续分析，减少掉进云端转写失败通道的概率。
- 优化语音失败提示文案，改成更准确的“当前语音转写暂不可用，可直接改用文字记录”。
- 调整高级模型说明文案，突出“更稳定的语音理解、更强的食物识别与营养分析能力”。
